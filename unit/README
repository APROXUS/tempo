Specify the install directory and service user:

		nano tempo.service
		nano lavalink.service
		
		Change the "User" and "WorkingDirectory"

Follow steps to activate as service:

	0.	sudo bash ../service.sh
	
			OR

    1.	sudo cp tempo.service /etc/systemd/system/tempo.service
		sudo cp lavalink.service /etc/systemd/system/lavalink.service
	   
    2.	sudo systemctl daemon-reload
	   
    3.	sudo systemctl enable tempo
		sudo systemctl enable lavalink

Finally, Start the server:

    	sudo systemctl start tempo
		sudo systemctl start lavalink

Check current status:

    	sudo systemctl status tempo
		sudo systemctl status lavalink

Stop the server:

    	sudo systemctl stop tempo
		sudo systemctl stop lavalink

Restart the server:

		sudo systemctl restart tempo
		sudo systemctl restart lavalink

View the server logs:

    	journalctl -f -u tempo
		journalctl -f -u lavalink